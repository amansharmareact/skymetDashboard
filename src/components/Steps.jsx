import React from "react";

const step = ()=>{
    return(
        <h2>Step 1: Use the map below to find villages with high crop readiness. Click on any green marker to zoom into its fields and view eligible parcels for collection.</h2>
    )
}

export default step;